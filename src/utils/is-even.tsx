/**
 * Verifica se um número é par.
 * @param num - O número a ser verificado.
 * @returns Retorna true se o número for par, caso contrário, false.
 */
export function isEven(num: number): boolean {
  return num % 2 === 0;
}
